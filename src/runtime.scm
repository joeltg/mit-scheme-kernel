(define *the-environment* (the-environment))
(load (merge-pathnames *source-pathname* "widget"))

; (load "widgets/slider")
; (load "widgets/progress")
; (load "widgets/text")
; (load "widgets/button")
; (load "widgets/checkbox")l